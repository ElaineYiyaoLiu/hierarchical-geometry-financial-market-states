# Golden cases

This directory holds small examples used to test the shared interface and metric implementations.

For the pilot interface, the suite includes valid examples for each schema and invalid cases covering missing required fields, incorrect sample order, asymmetric matrices, nonzero diagonals, non-finite values, checksum mismatch, and invalid recovered-tree leaves.

Cases should stay small enough to inspect by hand and deterministic enough to reproduce exactly.
