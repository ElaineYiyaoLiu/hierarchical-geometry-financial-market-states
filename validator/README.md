# Validator

Shared validation lives here. Both branches should run the same conformance checks before an artifact is exchanged.

The pilot suite will cover schema validity, sample ordering, matrix symmetry and diagonal conventions, finite values, checksums, and recovered-tree leaf validity. Valid and intentionally invalid examples live in `golden_cases/`.

The validation command and dependency versions will be fixed before pilot exchange.
