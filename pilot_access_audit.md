# Pilot access audit

Branch A restricted material stays in the private Branch A repository. The shared repository contains only Branch B-safe protocol/interface material. Branch B must not receive generator equations, hidden configurations, condition identities, latent labels/paths, true trees, leakage/noise values, or generator seeds through blind pilot packages.

The Project Lead controls exchange and release records but must not use raw truth-keyed pilot performance for generator or method tuning. The evaluation custodian / sealed-service path is required for the pilot escalation and permitted precision/resource release.

Current result: observable/restricted separation is implemented; pilot exchange is not yet recommended because Branch B and Faculty gates remain open.
